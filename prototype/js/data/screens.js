/* =========================================================
   Data: screen catalog + specs (+ Flutter tech + code hints for B2C/B2B)
   ========================================================= */
const SCREENS = {
  b2c: [
    { id:'b2c-splash',      name:'Splash',                 group:'Pre-auth', phase:1, purpose:'Brand moment; enter cold-start funnel.', states:['default','tap CTA'], notes:'Shown on first launch. Persist “hasSeenSplash” in local storage in real app.', tech:'Route: `/splash` (name: `b2cSplash`)\nFeature: `features/pre_auth` or `lib/screens/splash`\nDeps: `go_router`, `shared_preferences` / `flutter_secure_storage`\nBackend: none — local flag only.', codeHint:"// go_router\nGoRoute(\n  path: '/splash',\n  name: 'b2cSplash',\n  builder: (_, __) => const SplashScreen(),\n);\n\n// After animation\nfinal prefs = await SharedPreferences.getInstance();\nawait prefs.setBool('has_seen_splash', true);\ncontext.go('/language');" },
    { id:'b2c-lang',        name:'Language',               group:'Pre-auth', phase:1, purpose:'Choose app locale before onboarding.', states:['default','RTL preview'], notes:'English + العربية. Drives copy direction and strings.', tech:'Route: `/language`\nFeature: `features/pre_auth`\nDeps: `flutter_localizations`, `intl`\nBackend: optional `profiles.locale` sync post-login', codeHint:"// LocaleNotifier + MaterialApp\nlocale: ref.watch(localeProvider),\n\n// Persist choice\nawait ref.read(settingsRepo).setLocale(Locale('ar'));\n\n// RTL smoke test\nawait tester.pumpWidget(\n  wrapWithRtl(MaterialApp(home: LanguageScreen())),\n);" },
    { id:'b2c-onboarding',  name:'Onboarding (value props)', group:'Pre-auth', phase:1, purpose:'Explain map-first discovery and booking in 2–3 beats.', states:['step 1','step 2','step 3','skip'], notes:'Show once per install or until completed.', tech:'Route: `/onboarding` (PageView)\nFeature: `features/onboarding`\nDeps: `go_router`, `shared_preferences`\nBackend: none until account exists', codeHint:"final controller = PageController();\n\nonSkip() async {\n  await prefs.setBool('onboarding_done', true);\n  context.go('/auth');\n}\n\n// Page 0..2: ValuePropPage(step: n)" },
    { id:'b2c-auth',        name:'Auth — choose path',     group:'Pre-auth', phase:1, purpose:'Route to log in vs register.', states:['default'], notes:'No phone OTP. Email + password only on next screens.', tech:'Route: `/auth`\nFeature: `features/auth`\nDeps: `go_router`\nBackend: none on this shell', codeHint:"Column(\n  children: [\n    FilledButton(\n      onPressed: () => context.push('/login'),\n      child: Text('Log in'),\n    ),\n    OutlinedButton(\n      onPressed: () => context.push('/register'),\n      child: Text('Create account'),\n    ),\n  ],\n);" },
    { id:'b2c-login',       name:'Log in (email)',         group:'Pre-auth', phase:1, purpose:'Authenticate existing user with email + password.', states:['default','invalid credentials','loading'], notes:'Supabase Auth email/password. No SMS.', tech:'Route: `/login`\nFeature: `features/auth`\nDeps: `supabase_flutter`\nBackend: `supabase.auth.signInWithPassword`', codeHint:"final res = await supabase.auth.signInWithPassword(\n  email: emailCtrl.text.trim(),\n  password: passCtrl.text,\n);\nif (res.session != null) {\n  context.go('/map'); // or post-auth redirect\n}" },
    { id:'b2c-register',    name:'Register (email)',       group:'Pre-auth', phase:1, purpose:'Create account with email + password.', states:['default','weak password','email taken'], notes:'Optional verify-email flow post-MVP.', tech:'Route: `/register`\nFeature: `features/auth`\nDeps: `supabase_flutter`\nBackend: `supabase.auth.signUp`', codeHint:"await supabase.auth.signUp(\n  email: email,\n  password: password,\n  data: {'app_role': 'car_owner'},\n);\n// Handle AuthWeakPasswordException / duplicate user" },
    { id:'b2c-addcar',      name:'Add first car',          group:'Discovery', phase:1, purpose:'Onboard user with their vehicle so the map & tracker are useful.', states:['empty','saving','VIN lookup success/fail'], notes:'Skippable — user can browse map without a car, but can\'t book.', tech:'Route: `/garage/add` or modal\nFeature: `features/garage`\nDeps: `supabase_flutter`, optional VIN API client\nBackend: table `vehicles` (RLS: user owns row)', codeHint:"await supabase.from('vehicles').insert({\n  'user_id': supabase.auth.currentUser!.id,\n  'make': make,\n  'model': model,\n  'year': year,\n});\n\n// Skip → context.go('/map');" },
    { id:'b2c-map',         name:'Map — Home (CORE)',      group:'Discovery', phase:1, purpose:'Discover nearby verified service centers in <60s.', states:['loading pins','no results','location denied','emergency mode'], notes:'PostGIS query <within radius>. Bottom sheet shows top 3 pins. One-tap call on pin.', tech:'Route: `/map` (shell tab home)\nFeature: `features/map_discovery`\nDeps: `google_maps_flutter`, `geolocator`, `permission_handler`\nBackend: RPC e.g. `nearby_centers(lat, lng, radius_m)` or PostGIS view', codeHint:"final pos = await Geolocator.getCurrentPosition();\nfinal rows = await supabase.rpc('nearby_centers', params: {\n  'lat': pos.latitude,\n  'lng': pos.longitude,\n  'radius_m': 5000,\n});\n// Map pins from rows → Marker(markerId, position: LatLng(...))" },
    { id:'b2c-marketplace', name:'Parts marketplace',      group:'Discovery', phase:1, purpose:'Browse filters, oil, batteries, and accessories; compare sellers; cart placeholder.', states:['browse','search','empty cart','seller detail'], notes:'B2C parts layer — feed from catalog or partner API; checkout Phase 1 optional.', tech:'Route: `/marketplace`\nFeature: `features/parts_marketplace`\nDeps: `go_router`, list pagination\nBackend: `parts_listings` or external API gateway', codeHint:"final page = await supabase\n  .from('parts_listings')\n  .select()\n  .range(offset, offset + pageSize - 1);\n\n// Cart: local state / `cart_items` Phase 2" },
    { id:'b2c-filters',     name:'Filters sheet',          group:'Discovery', phase:1, purpose:'Narrow results by service, price, rating, open-now, distance, verified.', states:['default','applied','reset'], notes:'Filter chips on map header persist selection.', tech:'Route: modal/sheet off map (`showModalBottomSheet`)\nFeature: `features/map_discovery`\nDeps: Riverpod/Bloc for `MapFilters` state\nBackend: pass filter DTO into same `nearby_centers` or query builder', codeHint:"@freezed\nclass MapFilters with _$MapFilters {\n  const factory MapFilters({\n    Set<String>? services,\n    bool? openNow,\n    bool? verifiedOnly,\n  }) = _MapFilters;\n}\n\nref.read(mapFiltersProvider.notifier).state = next;" },
    { id:'b2c-search',      name:'Search results (list)',  group:'Discovery', phase:1, purpose:'Alt view to map — sortable list.', states:['default','empty','sorting'], notes:'Toggle between map/list.', tech:'Route: `/map/list` or sibling under map shell\nFeature: `features/map_discovery`\nDeps: same repo as map pins\nBackend: `centers_search` or filtered `nearby_centers`', codeHint:"ListView.builder(\n  itemCount: centers.length,\n  itemBuilder: (_, i) => CenterListTile(\n    center: centers[i],\n    onTap: () => context.push('/shop/${centers[i].id}'),\n  ),\n);" },
    { id:'b2c-shop',        name:'Service center profile', group:'Discovery', phase:1, purpose:'Decide whether to book here.', states:['default','closed now','no photos','unverified'], notes:'Verified badge is load-bearing for trust.', tech:'Route: `/shop/:centerId`\nFeature: `features/map_discovery`\nDeps: `go_router` path param\nBackend: `service_centers` + `reviews` aggregate', codeHint:"final row = await supabase\n  .from('service_centers')\n  .select('*, reviews(count)')\n  .eq('id', centerId)\n  .single();\n\ncontext.push('/book/${centerId}/service');" },
    { id:'b2c-tow',         name:'Tow truck profile',      group:'Discovery', phase:2, purpose:'View tow operator, ETA, coverage, fleet; request dispatch or call.', states:['default','busy','offline','surge pricing'], notes:'Roadside / tow supply (PRD Phase 4). Same booking shell can collect pickup + drop-off in production.', tech:'Route: `/tow/:operatorId`\nFeature: `features/roadside` (Phase 2)\nDeps: maps, tel `url_launcher`\nBackend: `tow_operators` + availability', codeHint:"launchUrl(Uri.parse('tel:${operator.phone}'));\n\n// Future: POST /edge-functions/tow-request\n// body: { operator_id, pickup, dropoff }" },
    { id:'b2c-service',     name:'Pick service',           group:'Booking', phase:1, purpose:'Choose the job to book.', states:['fixed price','range price','quote-only'], notes:'Quote-only routes to chat flow (phase 2) — for MVP use tap-to-call.', tech:'Route: `/book/:centerId/service`\nFeature: `features/booking`\nDeps: `go_router`\nBackend: `center_services` join `service_catalog`', codeHint:"final services = await supabase\n  .from('center_services')\n  .select()\n  .eq('center_id', centerId);\n\ncontext.push('/book/$centerId/slot', extra: selectedService);" },
    { id:'b2c-slot',        name:'Pick slot',              group:'Booking', phase:1, purpose:'Reserve a time from center\'s capacity.', states:['no slots today','held slot','held expired'], notes:'Edge function holds slot for 10 min via `booking_holds` row.', tech:'Route: `/book/:centerId/slot`\nFeature: `features/booking`\nDeps: call Edge Function via `supabase.functions.invoke`\nBackend: `booking_holds`, `center_availability`', codeHint:"await supabase.functions.invoke(\n  'hold_booking_slot',\n  body: {\n    'center_id': centerId,\n    'slot_start': slot.toIso8601String(),\n  },\n);\n\n// Poll or show countdown until hold expires" },
    { id:'b2c-payment',     name:'Review & pay',           group:'Booking', phase:1, purpose:'Confirm booking and pay (or defer to cash).', states:['card','cash on service','promo applied','payment failed'], notes:'MVP open question: cash-only vs in-app pay. Support both, default card.', tech:'Route: `/book/:centerId/checkout`\nFeature: `features/booking`\nDeps: payment SDK (Stripe) or cash path only\nBackend: `bookings` insert + payment intent edge fn', codeHint:"await supabase.from('bookings').insert({\n  'user_id': uid,\n  'center_id': centerId,\n  'service_id': serviceId,\n  'slot_start': slot,\n  'payment_method': 'cash_on_service',\n});" },
    { id:'b2c-confirmed',   name:'Booking confirmed',      group:'Booking', phase:1, purpose:'Reassure, give next steps.', states:['default','add-to-calendar'], notes:'FCM/APNS local reminder scheduled 1h before.', tech:'Route: `/bookings/:id/confirmed`\nFeature: `features/booking`\nDeps: `add_2_calendar` or platform channels\nBackend: read `bookings` row', codeHint:"final b = await supabase\n  .from('bookings')\n  .select()\n  .eq('id', bookingId)\n  .single();\n\n// schedule local notification\nawait flutterLocalNotificationsPlugin.zonedSchedule(...);" },
    { id:'b2c-bookings',    name:'My bookings',            group:'Booking', phase:1, purpose:'Track upcoming & past.', states:['empty','upcoming','in progress','past'], notes:'Realtime status via Supabase Realtime channel on booking_id.', tech:'Route: `/bookings` (tab)\nFeature: `features/booking`\nDeps: `supabase_flutter` Realtime channel\nBackend: `bookings` filtered by `user_id`', codeHint:"final channel = supabase.channel('booking:$id')\n  .onPostgresChanges(\n    event: PostgresChangeEvent.update,\n    schema: 'public',\n    table: 'bookings',\n    filter: PostgresChangeFilter(\n      type: PostgresChangeFilterType.eq,\n      column: 'id',\n      value: id,\n    ),\n    callback: (p) => ref.invalidate(bookingProvider(id)),\n  )\n  ..subscribe();" },
    { id:'b2c-progress',    name:'Booking in progress',    group:'Booking', phase:1, purpose:'Live status while the shop works.', states:['received','in progress','awaiting parts','ready for pickup'], notes:'Timeline component mirrors B2B status updates.', tech:'Route: `/bookings/:id/progress`\nFeature: `features/booking`\nDeps: same Realtime as `b2c-bookings`\nBackend: `booking_events` or status column on `bookings`', codeHint:"enum BookingStatus {\n  received, inProgress, awaitingParts, readyForPickup,\n}\n\nBookingTimeline(\n  steps: BookingStatus.values,\n  current: booking.status,\n);" },
    { id:'b2c-review',      name:'Rate & review',          group:'Booking', phase:1, purpose:'Capture post-service feedback.', states:['default','submitted','flagged'], notes:'Category ratings: quality, price, speed, honesty.', tech:'Route: `/bookings/:id/review`\nFeature: `features/reviews`\nDeps: form validation\nBackend: `reviews` insert (RLS: completed booking)', codeHint:"await supabase.from('reviews').insert({\n  'booking_id': bookingId,\n  'center_id': centerId,\n  'rating_quality': starsQuality,\n  'body': comment,\n});" },
    { id:'b2c-garage',      name:'Garage (cars)',          group:'Account', phase:1, purpose:'List + manage vehicles.', states:['empty','multi-car'], notes:'Mileage edit inline.', tech:'Route: `/garage`\nFeature: `features/garage`\nDeps: `go_router`\nBackend: `vehicles` list', codeHint:"final list = await supabase\n  .from('vehicles')\n  .select()\n  .eq('user_id', supabase.auth.currentUser!.id);" },
    { id:'b2c-cardetail',   name:'Car detail + history',   group:'Account', phase:1, purpose:'Service timeline + reminders.', states:['empty history','auto-logged','manual entry'], notes:'Auto-logged entries come from completed bookings — no duplicate UI.', tech:'Route: `/garage/:vehicleId`\nFeature: `features/garage`\nBackend: `service_history` join `bookings`', codeHint:"await supabase\n  .from('service_history')\n  .select('*, bookings(status, completed_at)')\n  .eq('vehicle_id', vehicleId)\n  .order('performed_at');" },
    { id:'b2c-dashboard',   name:'Dashboard',              group:'Account', phase:1, purpose:'Daily touch-point: health + reminders + recent.', states:['fresh user','active user','due soon'], notes:'"Due soon" card drives re-book flow.', tech:'Route: `/home` dashboard tab\nFeature: `features/home`\nBackend: aggregates — upcoming booking, reminders query', codeHint:"final upcoming = await supabase\n  .from('bookings')\n  .select()\n  .eq('user_id', uid)\n  .gte('slot_start', DateTime.now().toIso8601String())\n  .order('slot_start')\n  .limit(1);" },
    { id:'b2c-reminder',    name:'Reminder → Book',        group:'Account', phase:2, purpose:'Retention flow: push → 3 recommendations → book in 2 taps.', states:['default'], notes:'Smart reminders tied to booking history.', tech:'Route: deep link `/reminders/:id`\nFeature: `features/retention`\nDeps: `firebase_messaging` / FCM\nBackend: `reminder_campaigns` + edge cron', codeHint:"// onNotificationTap\ncontext.go('/map', extra: RecommendedCenters(ids));\n\n// From push data payload\nfinal centerIds = message.data['center_ids']?.split(',');" },
    { id:'b2c-expenses',    name:'Expenses (Phase 2)',     group:'Account', phase:2, purpose:'Fuel + expense tracker — retention.', states:['empty','populated'], notes:'Phase 2. Placeholder here for spec completeness.', tech:'Route: `/expenses`\nFeature: `features/expenses` (Phase 2)\nBackend: `expense_entries`', codeHint:"await supabase.from('expense_entries').insert({\n  'user_id': uid,\n  'amount': amount,\n  'category': 'fuel',\n  'spent_at': DateTime.now().toIso8601String(),\n});" },
  ],
  b2b: [
    { id:'b2b-splash',      name:'Splash (Business)',      group:'Pre-auth', phase:1, purpose:'Brand moment for service-center app.', states:['default'], notes:'Mirrors B2C cold start; separate binary or flavor in production.', tech:'Route: `/b2b/splash` (flavor: carcare_b2b)\nFeature: `features/b2b/pre_auth`\nDeps: `go_router`, `shared_preferences`\nBackend: none', codeHint:"GoRoute(\n  path: '/splash',\n  name: 'b2bSplash',\n  builder: (_, __) => const B2bSplashScreen(),\n);\n\ncontext.go('/b2b/language');" },
    { id:'b2b-lang',        name:'Language (Business)',    group:'Pre-auth', phase:1, purpose:'Locale for B2B UI.', states:['default'], notes:'English + العربية.', tech:'Route: `/b2b/language`\nFeature: `features/b2b/pre_auth`\nDeps: `intl`, app-specific ARB `app_b2b`\nBackend: optional `shop_profiles.default_locale`', codeHint:"MaterialApp(\n  locale: ref.watch(b2bLocaleProvider),\n  localizationsDelegates: AppB2bLocalizations.localizationsDelegates,\n);" },
    { id:'b2b-onboarding',  name:'Onboarding (Business)',  group:'Pre-auth', phase:1, purpose:'Explain bookings, catalog, verification for shops.', states:['step 1','step 2','step 3','skip'], notes:'Shop-focused value props.', tech:'Route: `/b2b/onboarding`\nFeature: `features/b2b/onboarding_wizard`\nDeps: PageView\nBackend: none', codeHint:"onComplete() {\n  context.go('/b2b/auth');\n}" },
    { id:'b2b-auth',        name:'Auth — choose path',     group:'Pre-auth', phase:1, purpose:'Log in vs create business account.', states:['default'], notes:'Email + password only on following screens.', tech:'Route: `/b2b/auth`\nFeature: `features/b2b/auth`\nDeps: `go_router`\nBackend: none on shell', codeHint:"context.push('/b2b/login');\ncontext.push('/b2b/signup');" },
    { id:'b2b-login',       name:'Log in (Business)',      group:'Pre-auth', phase:1, purpose:'Existing shop owner signs in with work email.', states:['default','invalid credentials'], notes:'Supabase Auth; role shop_owner.', tech:'Route: `/b2b/login`\nFeature: `features/b2b/auth`\nDeps: `supabase_flutter`\nBackend: JWT custom claim `app_role=shop_owner`', codeHint:"await supabase.auth.signInWithPassword(\n  email: email,\n  password: password,\n);\n\n// assert JWT has shop_owner\ncontext.go('/b2b/dashboard');" },
    { id:'b2b-signup',      name:'Register (Business)',    group:'Pre-auth', phase:1, purpose:'Create account with work email + password before business profile.', states:['default','email in use','weak password'], notes:'No phone field. Business/legal details on following onboarding steps.', tech:'Route: `/b2b/signup`\nFeature: `features/b2b/auth`\nBackend: `signUp` + metadata `app_role`', codeHint:"await supabase.auth.signUp(\n  email: email,\n  password: password,\n  data: {'app_role': 'shop_owner'},\n);\ncontext.go('/b2b/onboard/business');" },
    { id:'b2b-onboard-1',   name:'Onboarding — Business',  group:'Onboarding', phase:1, purpose:'Collect legal business info.', states:['default','invalid tax id'], notes:'Step 1 of 2.', tech:'Route: `/b2b/onboard/business`\nFeature: `features/b2b/shop_onboarding`\nDeps: forms, `image_picker` for license\nBackend: `shop_profiles` upsert + Storage bucket `verification_docs`', codeHint:"await supabase.from('shop_profiles').upsert({\n  'owner_id': uid,\n  'legal_name': legalName,\n  'tax_id': taxId,\n});\n\nawait supabase.storage\n  .from('verification_docs')\n  .upload('$uid/license.jpg', file);" },
    { id:'b2b-onboard-3',   name:'Onboarding — Catalog',   group:'Onboarding', phase:1, purpose:'Set services + pricing.', states:['default','bulk-add'], notes:'Prepopulate common services (oil, tires, brakes, AC).', tech:'Route: `/b2b/onboard/catalog`\nFeature: `features/b2b/catalog_setup`\nBackend: `center_services` bulk insert', codeHint:"await supabase.from('center_services').insert(\n  rows.map((e) => {\n    return {\n      'center_id': centerId,\n      'name': e.name,\n      'price_cents': e.priceCents,\n      'price_type': e.type.name,\n    };\n  }),\n);" },
    { id:'b2b-pending',     name:'Verification pending',   group:'Onboarding', phase:1, purpose:'Hold screen while admin reviews.', states:['pending','changes requested','approved'], notes:'Blocks map listing until approved.', tech:'Route: `/b2b/pending`\nFeature: `features/b2b/shop_onboarding`\nBackend: poll `shop_profiles.verification_status` or Realtime', codeHint:"supabase\n  .from('shop_profiles')\n  .stream(primaryKey: ['id'])\n  .eq('id', shopId)\n  .listen((rows) {\n    if (rows.first['verification_status'] == 'approved') {\n      context.go('/b2b/dashboard');\n    }\n  });" },
    { id:'b2b-dashboard',   name:'Dashboard (today)',      group:'Operations', phase:1, purpose:'Live ops: today\'s bookings + KPIs.', states:['empty day','busy day','realtime new'], notes:'Toast + sound on new booking via Realtime.', tech:'Route: `/b2b/dashboard` (tab)\nFeature: `features/b2b/operations`\nDeps: Realtime `postgres_changes` on `bookings`\nBackend: `bookings` where `center_id` + today filter', codeHint:"final stream = supabase\n  .channel('center-$centerId-bookings')\n  .onPostgresChanges(\n    event: PostgresChangeEvent.insert,\n    schema: 'public',\n    table: 'bookings',\n    filter: PostgresChangeFilter(\n      type: PostgresChangeFilterType.eq,\n      column: 'center_id',\n      value: centerId,\n    ),\n    callback: (_) => ref.invalidate(todayBookingsProvider),\n  )\n  ..subscribe();" },
    { id:'b2b-bookings',    name:'Bookings — calendar',    group:'Operations', phase:1, purpose:'Weekly view with assign/update.', states:['week','day','filter by mechanic'], notes:'Drag-drop reschedule in Phase 2.', tech:'Route: `/b2b/bookings`\nFeature: `features/b2b/operations`\nBackend: `bookings` range query by week', codeHint:"await supabase\n  .from('bookings')\n  .select()\n  .eq('center_id', centerId)\n  .gte('slot_start', weekStart.toIso8601String())\n  .lte('slot_start', weekEnd.toIso8601String());" },
    { id:'b2b-booking',     name:'Booking detail',         group:'Operations', phase:1, purpose:'Accept/reject/assign/update status + invoice.', states:['received','accepted','in progress','completed','rejected'], notes:'Status changes push to B2C in realtime.', tech:'Route: `/b2b/bookings/:id`\nFeature: `features/b2b/operations`\nBackend: update `bookings.status` — triggers B2C Realtime', codeHint:"await supabase.from('bookings').update({\n  'status': 'in_progress',\n  'assigned_mechanic_id': mechanicId,\n}).eq('id', bookingId);" },
    { id:'b2b-catalog',     name:'Catalog & pricing',      group:'Operations', phase:1, purpose:'Edit services, prices, promos.', states:['default','add service','quote-only'], notes:'Price types: fixed, range, quote.', tech:'Route: `/b2b/catalog`\nFeature: `features/b2b/catalog`\nBackend: `center_services` CRUD', codeHint:"await supabase\n  .from('center_services')\n  .update({'price_cents': newPrice})\n  .eq('id', serviceRowId);" },
    { id:'b2b-reviews',     name:'Reviews',                group:'Operations', phase:1, purpose:'Read + respond to reviews.', states:['default','flagged','responded'], notes:'Response is public, one per review.', tech:'Route: `/b2b/reviews`\nFeature: `features/b2b/reviews`\nBackend: `review_responses` upsert (1 per review)', codeHint:"await supabase.from('review_responses').upsert({\n  'review_id': reviewId,\n  'center_id': centerId,\n  'body': responseText,\n}, onConflict: 'review_id');" },
    { id:'b2b-more',        name:'More (B2B hub)',         group:'Operations', phase:1, purpose:'Reach reviews, payouts, and settings from bottom tab overflow.', states:['default'], notes:'Fourth tab; keeps primary nav uncluttered on phone.', tech:'Route: `/b2b/more` shell\nFeature: `features/b2b/shell`\nDeps: `NavigationBar` index 3', codeHint:"NavigationBar(\n  selectedIndex: 3,\n  destinations: [\n    /* dashboard, bookings, catalog, more */\n  ],\n);" },
    { id:'b2b-payouts',     name:'Payouts (Phase 2)',      group:'Operations', phase:2, purpose:'See earnings, commissions, payout schedule.', states:['pending','paid','on hold'], notes:'Commission % tied to tier (Free 15% / Pro 10% / Authorized 8%).', tech:'Route: `/b2b/payouts`\nFeature: `features/b2b/billing` (Phase 2)\nBackend: `payouts` + tier on `shop_profiles`', codeHint:"final payouts = await supabase\n  .from('payouts')\n  .select()\n  .eq('center_id', centerId)\n  .order('period_end', ascending: false);" },
  ],
  flutterGuide: [
    { id:'ftg-overview',         name:'Orientation & reading order',     group:'Start here', phase:1, purpose:'Onboard engineers to product surfaces, sources of truth (PRD, prototype, ADRs), and read-first topics.', states:['default','team RACI expanded'], notes:'Sources of truth: PRD, this prototype, ADRs. Surfaces: B2C mobile (map-first), B2B adaptive (tablet + phone), minimal admin + Supabase Studio. Ownership: ~2 B2C, ~2 B2B, 1 tech lead for shared packages + BaaS. Read first: email/password auth MVP, PostGIS map pins + filters, booking state + Realtime sync with B2B.' },
    { id:'ftg-repo-flavors',     name:'Repo layout, flavors, envs',      group:'Project setup', phase:1, purpose:'Document mono-repo shape, flavors, --dart-define / env matrix, and secrets handling (no keys in git).', states:['default','new machine checklist'], notes:'Example: apps/carcare_b2c, apps/carcare_b2b, packages/core, packages/ui, packages/features/*. Flavor matrix: bundle IDs, display names, API URL injection. CI variable naming convention.' },
    { id:'ftg-architecture',     name:'Layers & dependency rules',       group:'Architecture', phase:1, purpose:'Single agreed architecture so the team does not fork patterns across features.', states:['default','import cheat sheet'], notes:'Feature-first + Clean: Presentation (go_router, MVVM-style Notifier/Bloc) → optional Application use-cases → Domain (entities, repo interfaces, no Flutter/Supabase imports) → Data (Supabase impl, DTOs). Extract packages/features/* when stable. Errors: Result/Failure; user-facing vs log-only. Suggested packages: go_router, Riverpod or Bloc, supabase_flutter, freezed, maps + geolocator — see phone mock stack + pkg row.' },
    { id:'ftg-routing-deeplinks', name:'Navigation & deep links',        group:'Architecture', phase:1, purpose:'Global routing, tab shells, auth redirects, and deep links (booking, shop, push targets).', states:['logged out','logged in','invalid deep link'], notes:'Route tree + guest vs authenticated redirects. URL scheme / app links placeholders. Android back stack; state restoration expectations.' },
    { id:'ftg-state-di',         name:'State management & DI',           group:'Architecture', phase:1, purpose:'One primary pattern for async UI and testability (Riverpod or Bloc — pick one team-wide).', states:['loading','error','data'], notes:'Notifiers/Blocs per feature; repositories injected. When AsyncValue vs local ValueNotifier. Widget tests: provider overrides / bloc mocks.' },
    { id:'ftg-supabase',         name:'Supabase integration',            group:'Platform', phase:1, purpose:'Auth, Postgres + RLS, Realtime, Storage, Edge Functions (e.g. slot holds) from Flutter.', states:['signed out','session expired','realtime disconnected'], notes:'Client lifecycle, session refresh, sign-out cleanup. RLS assumptions per feature (pins, bookings, reviews). Realtime channel naming; B2B new-booking toast. Storage buckets for photos / verification docs.' },
    { id:'ftg-maps-location',    name:'Maps, geo, permissions',          group:'Platform', phase:1, purpose:'Map-first discovery aligned with prototype b2c-map: PostGIS results, pins, list toggle, emergency mode.', states:['loading pins','empty','permission denied','location off'], notes:'SDK choice (Google vs Mapbox) vs licensing. PostGIS response shape contract. Pin clustering, bottom sheet, perf budget.' },
    { id:'ftg-media-forms',      name:'Forms, validation, uploads',      group:'Platform', phase:1, purpose:'Booking funnel and B2B profile flows: validation, i18n messages, image pick → compress → Storage.', states:['validation errors','upload progress','retry after failure'], notes:'Shared patterns for VIN/catalog fields if reused across surfaces. Accessibility on form fields.' },
    { id:'ftg-ui-i18n-rtl',      name:'Design system, i18n, RTL',        group:'UX foundations', phase:1, purpose:'Material 3 + tokens from prototype CSS; English + العربية; RTL-critical layouts.', states:['LTR','RTL','large text / contrast'], notes:'Map ThemeExtension from design tokens. intl + ARB workflow. Golden tests for map chrome and booking in RTL.' },
    { id:'ftg-quality-release',  name:'Testing, CI/CD, observability',   group:'Delivery', phase:2, purpose:'Definition of done: lint, tests, build gates, crash reporting, store release.', states:['CI green','failing gate','hotfix policy'], notes:'Pyramid: unit (domain), widget, integration (Patrol or integration_test). PR pipeline: format, analyze, test, build. Crashlytics or Sentry + PII rules. Signing and versioning.' },
  ],
};
